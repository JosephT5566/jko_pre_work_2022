import { styled } from "@material-ui/core/styles"

const Button = styled("button")({
    padding: "0.5em 1em",
    border: "none",
    borderRadius: "0.5em",
    color: "white",
    background: "grey",
    cursor: "pointer",
    whiteSpace: "nowrap",
    textAlign: "center",
})

export default Button
